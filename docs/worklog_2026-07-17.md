# Worklog 2026-07-17

Running log of autonomous dev session (post-reboot). Newest entries at the bottom.

## Executive summary (as of ~13:30; cloud run + ro_dual verdict still in flight)

1. **GPU root cause**: the three killed jobs of 07-16 were an in-place NVIDIA driver update (NVML "driver/library version mismatch"); reboot fixed it. CUDA torch lives only in `.venv` (pyenv python is CPU-only).
2. **New champion, provisional (S16)**: covers champion +8 ep on the 152-song corpus (3,049 crops, LABEL_PRIOR_ALPHA=0.3, 12 min local). Greedy test SER 0.1808 (best ep 7). End-to-end: **SER 0.2570 → 0.2450**, timed-F1 0.7145 (−0.0011, noise), bF1@50 unchanged 0.7237. Gold arbitration: SER exactly tied (256 errors / 1,459 tokens for both), gold timed-F1 **+0.0054** for the candidate → promoted under the S11 precedent; awaiting ratification (one-line revert).
3. **Kaggle fully API-driven** (no more manual uploads): token auth (`~/.kaggle/access_token`; CLI 2.x ignores kaggle.json), dataset `alexzgu/kashi-pack-152` (803 MB) uploaded — the old pack belongs to unrelated handle `alexanderzgu` and isn't writable; `colab/kaggle_push.py` (canonical-notebook single source + JSON overrides + kernelspec injection) and `colab/kaggle_watch.py` (verbose polling, auto log/output fetch). Troubleshooting arc: v2 ERROR (papermill needs kernelspec → fixed), v3 validated the entire path — pack discovery, 152/152 preload, **crops: 3049 = exact local parity**, T4 ≈ local speed — with the blank-collapse guard correctly refusing FA on a 1-epoch model; v4 = first real job, 12-ep from-scratch corpus-152, RUNNING. Finding: HF_TOKEN secret does not survive API pushes (public-mirror fallback works).
4. **Tier-2 batch 3 drafted** (Opus subagent): true inventory is 7,951 excluded rows on non-test songs (T1 admission brought its own tail; old estimate was 1.8k). Drafted 2,422 blocks = **13,452 morae over 125 songs**, 100% inventory-valid, 383 LOW-CONF flagged, 96 correctly skipped (credits/symbols). Not applied — awaiting user review via the s13 player (packaging in progress).
5. **ro_dual QA launched** (Opus subagent, running): homophone-aware romaji↔kana cross-track agreement over ~81 parallel-track songs; verdict on whether it's an S12-class label-QA lever pending.
6. **Bookkeeping**: leaderboard `ctc_t1_152`; approaches.md updated (corpus-152 provisional champion at the phonetic level, covers demoted to prior champion); memory + SIGNOFFS updated; checkpoint commit `e9c55d75f` pushed. Session policies now in effect: Opus subagents freely with strict cleanup; no new subagent launches past 95% usage (resume after reset); machine shared with another session — footprint kept light.

## GPU restored

Reboot cleared the NVML driver/library mismatch (kernel module now 610.43.03, matching userspace). This mismatch — from an in-place driver update 1.6 days into uptime — was what killed the three long GPU jobs on 07-16 (mixed retrain, UVR model download, corpus-152 retrain epoch 2+). `torch.cuda.is_available()` is True only under `.venv/bin/python` (torch 2.13.0+cu130); the pyenv shim python has CPU-only torch 2.13.0. All GPU work must use `.venv`.

## Corpus-152 retrain (artifacts/ctc_t1)

Launch: `.venv/bin/python colab/run_nb.py artifacts/ctc_t1 '{"REUSE_DATA": true, "REUSE_MODEL": false, "REUSE_FA": true, "EPOCHS": 8, "CHECKPOINT": ".../ctc_covers/out/ctc_model", "LABEL_PRIOR_ALPHA": 0.3}'` — 8 epochs from the covers champion on the full 152-song corpus (145 train / 7 frozen test), 3,049 crops, 736 steps/epoch, ~90 s/epoch wall. Log: `artifacts/ctc_t1/train_152.log`.

Greedy (no-LM) test SER by epoch: 0.2045, 0.1948, 0.1908, 0.1899, 0.1874, 0.1885, **0.1808** (best, epoch 7), 0.1842. Best checkpoint saved to `artifacts/ctc_t1/out/ctc_model`. Covers champion probed ~0.20 on the same crops — the 59 admitted T1 songs (+66% train segments) are worth ~0.02 greedy SER.

## Adoption gate vs official baseline (SER 0.2570 / timed-F1 0.7156)

End-to-end spike decode on frozen test, batch-2-enriched refs (`artifacts/ctc_t1/eval_test.log`):

| metric | champion (ctc_covers) | candidate (ctc_t1) | Δ |
|---|---|---|---|
| SER | 0.2570 | **0.2450** | −0.0120 |
| timed-token F1 | 0.7156 | 0.7145 | −0.0011 |
| boundary@50ms F1 | 0.7237 | 0.7237 | 0 |
| partial credit | — | 0.9571 | — |

SER gate passes clearly; timed-F1 is flat (−0.15% relative). Same shape as S11 → gold subset arbitrated (`--gold --split train`; gold windows live on train songs 0/6/16/19 — a test-split gold eval is empty):

- Gold SER: **exactly tied** — both models make 256 edit errors on 1,459 gold-window tokens. Distribution differs: candidate 0.171/0.093/0.094/**0.390** vs champion 0.138/0.108/0.103/**0.438** per song (0/6/16/19) — candidate wins the hardest song, loses a little on the easiest.
- Gold timed-F1: 0.7414 → **0.7469** (+0.0054, favors candidate). Gold boundary@20/@50 F1 ~0.006 lower (counter-signal, noted).

**PROMOTED provisionally (S16, awaiting ratification)**: `ctc_model = artifacts/ctc_t1/out/ctc_model` in configs/default.toml; leaderboard row `ctc_t1_152`; SIGNOFFS.md Open S16. One-line revert if declined. approaches.md update delegated to an Opus subagent per convention.

## Kaggle integration progress

- Auth: new CLI 2.2.3 ignores old-style `~/.kaggle/kaggle.json`; the user's 37-char token is the new format → written to `~/.kaggle/access_token` (600). Verified: `kernels list -m` shows alexzgu's kernels.
- Pulled `alexzgu/ctc-bootstrap` (+metadata) → `runs/kaggle/ctc_bootstrap/`. Last cloud run 2026-07-14, status COMPLETE. Notebook is the same 13-cell bootstrap as `colab/ctc_bootstrap.ipynb` with an `IN_KAGGLE` branch: pack auto-discovery under `/kaggle/input/**` (extracted or zip), `HF_TOKEN` via `kaggle_secrets.UserSecretsClient` with graceful anonymous fallback, outputs zipped to `/kaggle/working/kashi/ctc_out.zip`. Metadata: T4 GPU, internet on, private, dataset `alexanderzgu/kashi-pack`.
- Gotcha found: the attached dataset (93 songs, 2026-07-14) belongs to a DIFFERENT handle (`alexanderzgu`) than the authed account (`alexzgu`, zero datasets) — this token cannot version it. Fix: new dataset `alexzgu/kashi-pack-152` (152 vocals + subtitles + tokens + split, 803 MB, staged at `artifacts/kaggle_pack_152/` via symlink-resolved rsync) — upload running in background.
- `runs/kaggle_watch.py` written: push → poll status (default 90 s, timestamped, state-change callouts) → on COMPLETE/ERROR print last 80 log lines + fetch outputs. Exit codes 0/1/2 (complete/failed/window-exceeded).
- Drift check: the uploaded Kaggle kernel is an OLDER notebook than `colab/ctc_bootstrap.ipynb` — it predates PSEUDO_* crop mixing, LABEL_PRIOR_ALPHA (weighted CTC loss path), SPEED_PERTURB, and the blank-collapse FA guard. Decision: single-source from the canonical notebook; never hand-edit kernel copies.
- `colab/kaggle_push.py` written: applies JSON overrides textually to the run-flags + config cells of the canonical notebook (same knobs `run_nb.py` overrides locally), stages `artifacts/kaggle_push/` with kernel-metadata.json (id `alexzgu/ctc-bootstrap` kept so the account-side HF_TOKEN secret attachment stays; T4, internet, private, dataset `alexzgu/kashi-pack-152`), pushes (push = upload + auto-start a version run), then hands off to `colab/kaggle_watch.py`. Both tools live in tracked `colab/` (`runs/` is gitignored); the pulled kernel copy was deleted after the diff.
- Next: when the upload lands → `kaggle_push.py '{"EPOCHS": 1, "REUSE_DATA": false, "REUSE_MODEL": false, "REUSE_FA": false}'` as a smoke run validating pack auto-discovery, GPU training, FA of all 152 songs, and ctc_out.zip packaging. Open option for real jobs: add the local champion checkpoint to the dataset so cloud runs can continue from it (`CHECKPOINT=/kaggle/input/.../ctc_model` works — from_pretrained accepts paths).

## Kaggle smoke run: fail #1 → fix → retry

- Upload finished; dataset processed server-side (`--dir-mode zip` archives auto-extracted: `subtitles/`, `vocals/`, 198+ files listed, new 509-byte split.txt). `datasets status` 403s during/after processing — use `datasets files` as the readiness probe instead.
- Smoke v2 pushed → **ERROR within ~90 s**: `papermill ... ValueError: No kernel name found in notebook and no override provided.` Cause: Kaggle executes notebooks with papermill, which hard-requires `metadata.kernelspec`; Colab-authored notebooks don't carry one, and the user's original upload only worked because the Kaggle web editor injects it on save. Raw API pushes must bring their own.
- Watcher behaved exactly as designed: QUEUED → ERROR state-change callouts, full traceback auto-fetched, exit code 1 propagated. Total quota burned: ~2 min.
- Fix: `normalize_nb()` in kaggle_push.py — inject `kernelspec` (python3) + `language_info`, bump nbformat_minor to 5, assign stable cell ids (silences the MissingIDFieldWarning). Smoke v3 pushed; watcher + an early-signal probe (fires when status leaves QUEUED) both running.

## Smoke v3: infra fully validated; guard correctly kills a 1-epoch run

v3 reached RUNNING and executed the notebook for real. Validation markers from the auto-fetched log: `DATA: /kaggle/input/kashi-pack-152` (attach + extracted-pack discovery), `preloaded 152/152 songs (96s)`, **`crops: 3049` — exactly the local count** (bit-level data-pipeline parity cloud vs local), T4 trained 736 steps in ~130 s (≈ local A3000 speed), epoch 1 test_SER 1.0000 → the **blank-collapse FA guard aborted the run as designed** (S9 lesson: from a base checkpoint, CTC looks dead until ~epoch 5 — a 1-epoch smoke can never pass the guard; the smoke design was wrong, not the system). ~7 min quota spent.

Two findings:
- **HF_TOKEN secret does NOT attach to API-pushed versions** (no "loaded from Kaggle secrets" line). Public-model fallback worked. If a gated model is ever needed: attach a tiny private dataset carrying the token, or have the user re-save once in the web editor; never bake the token into the tracked notebook.
- `kernels status` new-CLI parse quirk: fine; `datasets status` 403s — probe readiness with `datasets files`.

## ro_dual cross-track QA: DIRECTION CLOSED (measurement)

Subagent analysis (runs/ro_dual_qa.py + report): 138/152 songs have parseable parallel romaji tracks (corpus growth raised coverage from the assumed ~81). Homophone-aware alignment (は=わ, を=お, へ=え, じ=ぢ, ず=づ): **98.81% agreement over 70,289 aligned morae**; 131/138 songs ≥0.95. The disagreement mass is structural (13.7k romaji-only insertions = backing vocals + English; labels exclude those). Clean isolated substitutions: just 82 across train songs — and every interpretable one is a **romaji-side artifact** (kana-ized proper nouns like マリン↔`marine`, song 35's phantom-`d` glitch, dropped consonants). Zero genuine label errors surfaced; an S12-style automated correction rule would inject errors, not fix them. Verdict: NOT a QA lever, NOT a Tier-2 source; keep per-song agreement as a sanity metric (songs <0.85: 21, 60, 85-TEST — all English/structural). Falsified prior belief: romaji tracks DO carry English (77/138 tracks, 1,887 Latin tokens). Leaderboard row `ro_dual_qa`.

## Tier-2 batch 3: drafted, validated, packaged for review

Subagent drafted **2,422 blocks = 13,452 morae across 125 non-test songs** (the real excluded-row inventory is 7,951 — the T1 admission brought its own tail; the old ~1.8k estimate was pre-admission). 100% of drafted morae are inventory-valid; keys cross-check against real blocks (draft generation consumed all 2,422 with zero misses). 383 blocks tagged LOW-CONF (English-fallback fragments, pykakasi kanji readings on rare okurigana, song 87's romanized Greek). Skipped correctly: 78 credit/metadata crawls, 15 unmappable, 3 symbol-only.

Packaged at `s13_batch3/`: pilot player + volume-sorted data.js (125 songs, top ~20 carry most mass), `[!N low-conf]` title badges, media fallback chain (dataset webm → imported webm → admission mp3 for the 2 pool-sourced songs). NOTHING applied to data/ — awaiting user review per the S13 process.

## First real cloud job: 12-epoch from-scratch corpus-152 (v4)

Replaces a third smoke — validates the remaining FA + ctc_out.zip stages AND is the natural ablation: current recipe (LABEL_PRIOR_ALPHA=0.3) from the yky-h base on all 152 songs, vs the incremental-continuation champion (greedy 0.1808, curriculum-history baggage). S9's 12-ep from-base on 93 songs hit greedy 0.198; corpus-152 from-base should beat it. ETA ~50 min T4; watcher at 120 s + early-signal probe armed.

**Result: BLANK-COLLAPSED THROUGH ALL 12 EPOCHS** (loss 4.34, test_SER 1.0000 at every epoch; guard refused FA — correctly). S9's from-scratch 12-ep on the same base reached 0.198 with collapse breaking ~ep 5. The single recipe delta vs S9: LABEL_PRIOR_ALPHA=0.3, which was only ever validated on WARM continuations (deprior era onward). **Hypothesis: the de-peaking prior term blocks CTC's cold-start alignment discovery** — early on, all probability mass sits on blank, the EMA prior tracks that, and the −α·log(prior) bonus for non-blank classes scrambles the fragile alignment-search dynamics instead of regularizing a formed solution. v5 pushed: identical run with alpha=0.0 (the true S9 replication) — one variable isolated on the same hardware. If v5 collapses too, the suspect becomes T4/environment (fp16 GradScaler), investigated next with DEBUG_MAX_SONGS. ~35 min quota per attempt; ~45 min used today of the ~30 h/wk.

**v5 result: ALPHA HYPOTHESIS DEAD.** alpha=0 collapsed identically — loss flat at ~4.33 (the all-blank plateau) from ep 6 through 12, test_SER 1.0 throughout, same as v4's 4.34. Cloud from-scratch collapses regardless of the regularizer.

Hypothesis matrix now:

| | 93 songs | 152 songs |
|---|---|---|
| local, from scratch | S9: SUCCESS (greedy 0.198, collapse broke ~ep 5) | **discriminator RUNNING** (artifacts/ctc_scratch152, alpha=0) |
| cloud, from scratch | untested | v4 (α=.3) + v5 (α=0): COLLAPSE, flat 4.33 |
| local, warm | success (entire champion lineage) | success (0.1808 greedy) |
| cloud, warm | — | untested; kashi-ckpt dataset now uploaded to enable it |

If the local 152-scratch succeeds → environment delta (cloud pip installs latest transformers; T4 is pre-Ampere so autocast fp16 vs any bf16 path; GradScaler skip-storms) → probe by pinning the local transformers version in the cloud install. If it collapses → the T1 tranche hurts cold-start alignment discovery specifically (warm runs tolerate it) → probe with DEBUG_MAX_SONGS subsets. OPERATIONAL NOTE: production cloud jobs are warm continuations from kashi-ckpt anyway — a warm cloud run both sidesteps the cold-start issue and completes the FA+zip validation; queue it after the discriminator readout.

## Matrix resolved: cloud env causes cold-start collapse; production loop fully validated

- **Local from-scratch corpus-152 SUCCEEDS** (`artifacts/ctc_scratch152`): collapse breaks at ep 4 (loss 2.80), greedy test SER 0.2108 at ep 12, FA completed. With cloud v4 (α=.3) and v5 (α=0) both flat at 4.33, the cause is the **cloud environment** — unpinned `transformers` (cloud resolves ">=4.44" to the image's latest; local is 5.13.0) and/or T4 fp16 — not the T1 data, not the regularizer.
- **Cloud warm v6 COMPLETE — the full production loop works**: warm +4 ep from `kashi-ckpt` → guard passed → FA 152 songs → ctc_out.zip 347 MB → auto-fetched by the watcher. Greedy best 0.1842 (ep 3) vs champion 0.1808 = the documented same-data continuation oscillation; not a candidate, wasn't the point. Zip + extracted model deleted post-triage (cleanup rule); run log kept at `artifacts/kaggle_out/ctc-bootstrap.log`.
- **Secondary finding**: cold-start 0.2108 vs warm-lineage 0.1808 on identical data/steps-scale — the incremental curriculum (bootstrap→pseudo→covers→corpus-152) carries a ~0.03 greedy edge; training history is load-bearing, not just data volume.
- Mitigation shipped: canonical notebook now pins `transformers==5.13.0`; **v7** (pinned cold-start, α=0) is running to isolate version-vs-fp16 as the specific mechanism. Leaderboard rows: `ctc_scratch152_local`, `ctc_cloud_warm4`.

## S17 opened: dataset re-evaluation + phonetic (as-sung) label space (user directive)

User (2026-07-17 evening): labels must serve karaoke use — the SUNG form always (particle は labeled わ, likewise を/へ), minimal linguistics except unavoidable English; and a general quality re-evaluation of all datasets incl. gold (GIGO check). Consequences:

- Today's labels are ORTHOGRAPHIC — the model is being trained to output spelling (hear [wa], write は). ro_dual's 1,505 homophone "folds" (は▸わ 682, を▸お 635, へ▸え, づ▸ず) flip from scoring convenience to RELABEL WORKLIST, instance-precise on 138/152 songs. Batch-3 drafts already follow as-sung convention — consistent.
- Two Opus audit agents launched:
  1. Phonetic census (runs/s17_phonetic_census.md + s17_relabel_worklist.csv): per-instance worklist with a high-precision "flanked" subset; counts of genuinely-orthographically-sung instances (singers do sometimes sing を as [wo] — decides blanket rule vs instance-level relabel); gold_v2 convention audit; non-ro-covered songs quantified; test-song counts kept separate (frozen refs — user decision, S12 precedent); SER-impact estimate.
  2. T1 + audio health QA (runs/s17_t1_audio_qa.md): furigana-conversion diffs on 30 sampled songs + full timing/silence sanity pass on all 152; tranche stats table; vocals-separation health (duration/RMS/near-silence outliers); duplicate/leak checks incl. frozen-test integrity.
- After audits: S17 migration plan (clean_v3, backups, re-baseline, retrain) goes to the user for sign-off. NO data changes until then.

## S17 audits landed; plan delivered; v7 confirms the pin

- Census: 1,405 relabel instances / 1,269 flanked auto-applicable; は/へ 50/55% genuinely orthographic → instance-level only; を/づ/ぢ blanket-safe; gold orthographic (~22 tokens); test counts separated (22 ro-verified on 81/83/85; 89–92 need the user's ear). Eval impact 1.73% of corpus tokens; refs-only relabel ≈ +0.006 test SER until retrain.
- GIGO audit: audio fully healthy (0/152 vocals flags), conversion faithful; defects concentrated in T1 (661 mangled ー/small-kana rows from an admit.py gate bug; 249 overlapping rows / 19 songs; 2 zero-duration silences in id 52); duplicate trio 39/58/64 (same song, 3 languages, all train); **Kotone singer contamination**: test 89–92 all Kotone + 6 different-ytid Kotone recordings in the pool bypass the ytid leak filter → blocklist + reporting caveat proposed.
- `docs/s17_phonetic_plan.md` written; sign-offs S17a/b/c open.
- **v7 COMPLETE: pin confirmed.** transformers==5.13.0 restores T4 cold-start escape — collapse breaks ep 10 (vs ep 4 locally; fp16 delays ~6 epochs but doesn't block), greedy 0.2302@12ep still descending, guard/FA/zip all passed. Unpinned image transformers was the blocker. Leaderboard `ctc_cloud_pin_v7`; fetched payloads deleted post-triage. Kaggle quota today ≈ 3.3 h.

## Kaggle API integration (in progress)

Goal: push/run/monitor Kaggle notebooks directly so the user never hand-uploads. State:

- `kaggle` CLI 2.2.3 installed into `.venv` via `uv pip install kaggle`.
- Credentials: `~/.kaggle/kaggle.json` absent. User (AlexZGu) has the API key on their clipboard; instructed to pipe clipboard → `~/.kaggle/clip.tmp` via `wl-paste` so the key never enters the chat transcript; I normalize it to `kaggle.json` (wrap bare key with username) at 600 perms and delete the temp file.
- Once authed: `kaggle kernels pull` the uploaded ctc-bootstrap notebook as reference, wire `HF_TOKEN` via `kaggle_secrets.UserSecretsClient` (secret already attached to the notebook on Kaggle), then build a push → poll `kernels status` → fetch `kernels output`/`logs` watcher with timestamps and frequent check-ins for troubleshooting.
