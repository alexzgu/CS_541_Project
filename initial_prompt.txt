The current working directory is `/home/eizigi/Documents/GitHub/CS_541_Project`. You have access to this and `/home/eizigi/Documents/GitHub/karaoke_subtitle_dataset`. DO NOT look in the `/home/eizigi/Documents/GitHub/CS_541_Project/presentation` folder; there's simply no need to.\n\nThis is an old project that I want to revisit. For context, refer to the contents of the report in `/home/eizigi/Documents/GitHub/CS_541_Project/Deep_Learning___JP_KRKE_Subtitle_Transcription.zip` and explore the contents of the folders and files in this repository. For the data, I suggest you look at a few examples to get the idea, then refrain from looking at the others until needed.\n\nFirst of all, I want some refactoring done to the underlying codebase, as it is a bit of a mess; I would like a clean set of commands that handles the most essential functionalities. Second, I want a system where I can easily swap in/out the different models used; e.g., the vocal-instrumental separator. Second, I never got to fully clean through the dataset, as the premise was that I would put in enough time to clean the slightly-misaligned labels and properly label distractions in the audios (e.g., sounds of breathing in), but that in retrospect would take too much time to finish.\n\nSome other things I also want to do/address include:\n- making an easy-to-use web app, where I can insert an mp4 music video (WITHOUT transcription labels), and the full pipeline will run through it to produce accurate subtitles.\n- I want to actually implement some way so that whatever models we use can actually train on unsupervised data.\n- This concerns the stuff mentioned above: I want to explore alternative ways of approaching this problem via statistical learning techniques. For this, see `/home/eizigi/Documents/GitHub/CS_541_Project/context-summary-bayesian-nonparametrics-gp-tda.md` and the below chat log.\n\n/* CHAT LOG BEGINS */\nME: Suppose I want to reattempt this problem with one or more of the approaches we've discussed so far. This also includes the part about continuing the project by implementing contrastive learning, or at least a more practical and principled version.\n\nCLAUDE: The GitHub blob link only returns the page wrapper, not the PDF itself. Let me pull the raw file directly and read it.Read the whole paper. Before mapping tools onto it, the key move is to **re-diagnose the two failures structurally** — because the methods we discussed don't attach to \"Japanese karaoke,\" they attach to the *structure* of what went wrong. And both failures are structural, not tuning problems.\n\n## Re-Diagnosing the Two Failures\n\n**Segmentation (Model 1, F1 ≈ 0.41) didn't fail from bad hyperparameters — it failed from a model-class mismatch.** You framed boundary detection as *per-frame independent binary classification* (\"break / no break\" on each 20-ms frame). That framing **manufactures** the 4–6% imbalance you fought, because a syllable boundary is an *event*, and events are sparse by definition. Padding the labels and dropping the threshold to 0.4 are patches on a framing that throws away the two things that actually constrain boundaries: that segments **persist** (a syllable spans many frames) and that the syllable **sequence is known** even when the timing isn't.\n\n**Prediction (Model 2, micro-F1 ≈ 0.535) genuinely worked — but it's bottlenecked by an unstructured label space.** Your own error analysis says the mistakes cluster on *wo/o* and *ka/ga* — i.e., voicing and near-homophones. BCE treats all 110 classes as equidistant, so it can't see that *ka*→*ga* is a near-miss and *ka*→*n* is a disaster. That's exactly the \"partial credit\" intuition in your Future Work 7.2.\n\n**The contrastive continuation (7.1) is, in our vocabulary, \"learn the feature map $g_w$\"** — the same $g_w$ that deep kernel learning bolts onto a GP. You weren't really planning to \"add contrastive learning\"; you were planning to *learn the kernel* over audio from unlabeled data. Naming it that way tells us how to do it principledly.\n\nHere's the mapping, in the structural-match style from last turn:\n\n| Subproblem | Structural signature | Matched method (ours) | Fit |\n|---|---|---|---|\n| Boundary segmentation (failed) | segment a stream into an unknown # of *persistent* units; boundaries latent; frame labels noisy | **sticky HDP-HMM** / Bayesian semi-Markov | **strong** |\n| Leverage unlabeled audio (7.1) | learn *what's invariant* — the feature map | **contrastive = deep-kernel feature learning** | **strong** |\n| Graded label space (7.2) | output space with *similarity structure* | **phonetic kernel on labels** | **strong** |\n| *ka/ga*, *wo/o* confusions | voiced = quasi-periodic vs unvoiced = aperiodic | **TDA delay-embedding ($H_1$)** | narrow/genuine |\n| ±50 ms label jitter | latent monotonic time-warp | GP warp (or absorbed by HMM) | narrow |\n| Onsets as events | temporal point process | Hawkes | forced |\n\nLet me develop the strong fits, then honestly triage the rest — forcing every tool would violate the exact principle we built.\n\n---\n\n## 1. Segmentation → Sticky HDP-HMM (the centerpiece)\n\nThis is the most direct payoff of the entire early thread. Recall from the processes discussion: **HDP-HMM → segmentation with an unknown number of states, inferred while keeping temporal dynamics** — and it's the tool behind speaker diarization. Acoustic unit discovery is the same problem as yours, and the Bayesian-nonparametric treatment (Lee & Glass-style DP-HMMs, Fox et al.'s **sticky** HDP-HMM) was built for it.\n\nWhy it dissolves your two segmentation problems at once:\n\n- **The imbalance vanishes because you stop doing per-frame binary classification.** Boundaries are no longer a label you predict at each frame; they're the *latent change-points* where the HMM switches state. There is no \"4–6% positive class\" to balance — the model emits a segmentation, not a per-frame coin flip. The \"sticky\" self-transition bias (Fox et al.) is *literally the cure for your symptom*: it encodes that states persist for many frames, which is the structural fact your per-frame classifier had to fight against.\n- **The ±50 ms jitter stops mattering because alignment becomes latent.** You have the reliable thing (the *syllable sequence* from the subtitles) and the unreliable thing (frame-precise *timestamps*). The HMM uses the sequence as a constraint and **infers** the boundaries from the audio via forward-backward — which is exactly forced alignment, but driven by the acoustics rather than trusting noisy 20-ms timestamps. The breathing-in-\"silent\"-segments issue (Figure 3) softens too, because a silence state is defined by its emission statistics, not by a label you over-trusted.\n- **The inference is the machinery you already learned.** The HDP gives a CRP-style prior over how many units exist; posterior inference is Gibbs sampling over assignments and boundaries — the CRP → DPMM → Gibbs arc from the start of our conversation, now doing real work. Two modes are available: *supervised* (states tied to your 110 Hiragana, so it's robust alignment) or *unsupervised* (let the HDP discover phone-like units and compare them to your inventory — genuine acoustic unit discovery).\n\nThis single change addresses Inherent Dataset Flaws (6.1) at the modeling level instead of \"manually hacking the timings\" (which you rightly deemed too costly).\n\n---\n\n## 2. The Contrastive Continuation, Done Principledly\n\nReframed as \"learn $g_w$,\" the whole question becomes: **what should count as a positive pair, and what as a negative?** That single choice *is* the inductive bias. Generic SimCLR audio augmentations encode generic invariances (noise, masking) — fine, but not aimed at *your* nuisance variables. The principled version aims them:\n\n**Positives from covers — your Future Work 7.3, elevated from data augmentation to the core of the objective.** Different renditions of the same song carry the *same syllable sequence* sung by *different voices*. So a cover pair gives you \"same syllable, different singer/timbre\" — which means the contrastive objective learns an embedding **invariant to singer and timbre but sensitive to syllable identity.** That is precisely the invariance transcription needs, and it's exactly the nuisance a single-rendition model can't isolate. This is far more principled than borrowed augmentations because the invariance is *defined by the task's actual confound*, not guessed. The catch — covers must be aligned first — is handled by the HDP-HMM aligner from §1, so the two pieces **feed each other**: alignment yields positives; better positives yield features that sharpen alignment.\n\n**Negatives weighted by a phonetic kernel — which also solves 7.2.** Naive contrastive learning pushes *all* non-matching pairs maximally apart, which is wrong here: it would try to shove *ka* and *ga* to opposite corners when they genuinely differ only in voicing. Define a kernel $k_{\\text{phon}}(\\cdot,\\cdot)$ over the 110 syllables from articulatory features (voicing, place of articulation, vowel), and use it twice:\n\n$$k_{\\text{phon}}(\\text{ka},\\text{ga}) \\approx \\text{high (one voicing bit apart)}, \\qquad k_{\\text{phon}}(\\text{ka},\\text{n}) \\approx \\text{low}$$\n\n- In the **contrastive loss**, down-weight the repulsion between phonetically close syllables (soft negatives), so the embedding geometry mirrors phonetic geometry instead of fighting it.\n- In **Model 2's loss**, replace flat BCE/cross-entropy with a $k_{\\text{phon}}$-weighted (\"partial credit\") loss — *ka*→*ga* incurs small loss, *ka*→*n* large. This is your 7.2 made concrete, and it's the same object as the soft-negative weighting. **One phonetic kernel serves both the representation learning and the classifier loss**
— the kind of reuse the kernel viewpoint keeps buying us.\n\nThat's the \"more practical and principled version\": not \"do contrastive learning,\" but *specify the exact invariance (timbre, via covers) and the exact metric (phonetics, via a kernel) the objective should encode.*\n\n---\n\n## 3. The One Genuine Slice of TDA: Voicing via Periodicity\n\nWhole-pipeline TDA would be a forced fit, and saying so is the discipline we agreed on. But there is **one** place its bias is exactly right — and it happens to target your dominant Model-2 error. Voiced sounds (vowels, *ga*, *wo*) are **quasi-periodic** (glottal pulses at the fundamental frequency); unvoiced sounds and silence are **aperiodic**. Under a time-delay (Takens) embedding, a periodic signal traces a **closed loop** → a persistent $H_1$ bar; aperiodic signal → no loop. So a cheap per-frame topological feature — \"is there a persistent $H_1$ class in the local delay embedding?\" — is a **principled voiced/unvoiced detector**, appended to the Wav2Vec2 vector. That's precisely the *ka*/*ga*, *wo*/*o* axis your confusion matrix lights up on. Narrow, but a real structural match rather than a forced one.\n\n---\n\n## 4. Honest Triage of the Rest\n\nThe remaining tools are narrow-to-forced here, and the principled answer is to scope them tightly:\n\n- **GP — not the main classifier.** 110 classes over millions of frames is exactly the high-dimensional, data-rich regime where we said NNs beat GPs; a GP softmax would be the wrong tool. Its *genuine* niches: (a) a **monotonic-GP prior over the label-time → audio-time warp**, a principled way to absorb the ±50 ms jitter if you prefer an explicit warp to the HMM's latent alignment; (b) calibrated uncertainty on segment predictions. Useful, narrow, not the spine.\n- **DPP — diverse selection, nothing more.** Two legitimate slots: mining **diverse hard negatives** for contrastive batches (so negatives span the phonetic space rather than clumping), and choosing a **non-redundant subset of songs** to prioritize under a limited labeling/cleaning budget. Real, but peripheral.\n- **Hawkes — framing right, mechanism wrong.** Viewing onsets as a temporal point process is the correct abstraction, but Hawkes' *self-excitation* (one event raising the rate of the next) models contagion, and singing rhythm is **quasi-periodic**, not contagious. The HDP-HMM's duration/transition model captures the temporal structure better. Take the point-process *framing*, leave the specific Hawkes intensity.\n\n---\n\n## The Unified Architecture\n\nThe pieces interlock rather than stack, and the synergies are the point:\n\n$$\\underbrace{\\text{covers} \\to \\text{contrastive } g_w}_{\\text{§2: learn the feature map}} \\;\\to\\; \\underbrace{\\text{sticky HDP-HMM}}_{\\text{§1: joint align + segment}} \\;\\to\\; \\underbrace{k_{\\text{phon}}\\text{-weighted classifier}}_{\\text{§2/§4: graded loss}}$$\n\nwith the **TDA voicing bit** (§3) concatenated into the features, **GP-warp** (§4) optional for jitter, and **DPP** (§4) feeding diverse negatives back into §2. The reinforcing loop is what makes it more than a checklist: the **HMM aligner produces the cover alignments that generate contrastive positives**, the **contrastive features make the HMM's emissions cleaner**, and the **phonetic kernel serves both the contrastive negatives and the classifier's partial-credit loss.** Each of your three Future-Work items maps to one principled mechanism instead of three ad-hoc fixes.\n\n---\n\n## Key Takeaways\n\n1. **Both failures were structural, not tuning.** Segmentation failed because per-frame binary classification *manufactures* the sparsity problem and trusts noisy timestamps; the fix is a *segmental* model where boundaries are latent — the sticky HDP-HMM, run on the CRP→Gibbs machinery from the start of this thread.\n2. **Your contrastive plan is \"learn the kernel.\"** Make it principled by choosing positives from **covers** (timbre invariance — the task's real confound) and weighting negatives by a **phonetic kernel** — which simultaneously delivers the \"partial credit\" loss you wanted for Model 2.\n3. **One phonetic kernel, three jobs:** soft contrastive negatives, graded classification loss, and a phonetic-geometry prior on the embedding. The reuse is the kernel viewpoint paying off again.\n4. **Discipline over enthusiasm.** HDP-HMM and contrastive feature learning are *strong* structural fits; TDA earns exactly one narrow slot (voicing via periodicity); GP, DPP, and Hawkes are narrow-to-forced and scoped accordingly. Matching tools to structure — not to the domain — is the whole lesson, applied to your own project.\n\nIf you want to take one further, the natural next step is to write out the **sticky HDP-HMM generative model** concretely for your setup — states, sticky transition prior, Wav2Vec2 emission model, and the Gibbs step that infers boundaries — so you can see exactly how the timing jitter and imbalance dissolve in the math. That's the piece that turns this plan into something implementable.\n\nME: Yes\n\nCLAUDE: The capstone. I'll build the model in the four pieces you named, run the inference, then show the two failures dissolving — with a small concrete alignment at the end so it's not just symbols.\n\nFirst, the one reframe that everything hangs on:\n\n> A syllable boundary is **not a label you predict**. It is the derived event $z_t \\neq z_{t-1}$ — a *consequence* of an inferred state path. \"Break / no-break\" disappears as a classification target and reappears as a side-effect of segmentation.\n\nHold onto that; it's why the imbalance evaporates.\n\n---\n\n## The Generative Model\n\nNotation for your setup: frame index $t = 1, \\dots, T$ (each frame = 20 ms), emission $x_t \\in \\mathbb{R}^{768}$ (the Wav2Vec2 vector), hidden state $z_t \\in \\{1, 2, \\dots\\}$ (a syllable-unit). Four ingredients.\n\n### (i) States — the shared menu (HDP top level)\n\nThe infinite inventory of units is drawn once, globally, by stick-breaking — the \"franchise menu\" from the Chinese restaurant franchise you met early on:\n\n$$\\beta \\sim \\text{GEM}(\\gamma)$$\n\n$\\beta = (\\beta_1, \\beta_2, \\dots)$ is a distribution over the infinite state space; $\\beta_k$ is unit $k$'s global prevalence, and $\\gamma$ controls how many units get appreciable mass. **You don't fix the number of syllable-units — $\\beta$ does, from data.**\n\n### (ii) Sticky transitions — the crucial ingredient\n\nFor each state $k$, a transition distribution over next states, drawn from a DP centered on $\\beta$ but with an added self-transition mass $\\kappa$ (Fox et al.'s \"sticky\" term):\n\n$$\\pi_k \\sim \\text{DP}\\!\\left(\\alpha + \\kappa,\\; \\frac{\\alpha\\,\\beta + \\kappa\\,\\delta_k}{\\alpha + \\kappa}\\right), \\qquad \\rho \\equiv \\frac{\\kappa}{\\alpha+\\kappa} = \\text{expected self-transition bias}$$\n\nThat $\\kappa\\,\\delta_k$ is doing direct battle with your failure mode. A vanilla HDP-HMM switches states almost every frame (it would shatter one syllable into dozens of micro-units); $\\kappa$ encodes **\"states persist for many frames,\"** which is the structural truth that a syllable spans ~10–30 frames. The sticky bias is the principled form of the fact your per-frame classifier had to fight.\n\n### (iii) Wav2Vec2 emissions\n\nEach state owns a Gaussian over the 768-dim embedding space, with a conjugate Normal-Inverse-Wishart prior (conjugacy buys closed-form Gibbs updates):\n\n$$\\theta_k = (\\mu_k, \\Sigma_k) \\sim H = \\text{NIW}(\\mu_0, \\lambda, \\Psi, \\nu), \\qquad x_t \\mid z_t = k \\;\\sim\\; \\mathcal{N}(\\mu_k, \\Sigma_k)$$\n\nPractical note that closes the loop with last turn: a full $768\\times768$ covariance is heavy, so in practice use diagonal $\\Sigma_k$, or — better — **emit in the contrastive feature space $g_w(x_t)$ from the previous step** rather than raw Wav2Vec2. The contrastive map gives lower-dimensional, more Gaussian-shaped clusters (it was *trained* to make same-syllable frames cluster), so the emission model fits far better. The two proposals reinforce each other exactly here.\n\n### (iv) The sequence\n\n$$z_t \\mid z_{t-1} \\sim \\pi_{z_{t-1}}, \\qquad x_t \\mid z_t \\sim \\mathcal{N}(\\theta_{z_t})$$\n\n**Compact generative story:**\n\n$$\\beta \\sim \\text{GEM}(\\gamma); \\quad \\pi_k \\sim \\text{DP}(\\dots) \\text{ and } \\theta_k \\sim H \\;\\;\\forall k; \\quad z_t \\sim \\pi_{z_{t-1}}; \\quad x_t \\sim \\mathcal{N}(\\theta_{z_t})$$\n\nBoundaries are read off afterward as $\\{t : z_t \\neq z_{t-1}\\}$. Nowhere is a \"break\" ever a modeled random variable.\n\n---\n\n## Inference: Blocked Gibbs\n\nDirect per-frame Gibbs on $z_t$ mixes terribly here (adjacent states are strongly coupled). The standard fix (Fox et al.) is the **weak-limit approximation** — truncate to $L$ states ($L$ a generous upper bound, or $L=110$ if you tie states to your inventory) — which turns the DPs into finite Dirichlets and unlocks **forward-filter / backward-sample (FFBS)**, letting you resample the *entire* path at once:\n\n$$\\beta \\sim \\text{Dir}\\!\\big(\\tfrac{\\gamma}{L}, \\dots, \\tfrac{\\gamma}{L}\\big), \\qquad \\pi_k \\sim \\text{Dir}\\big(\\alpha\\beta_1, \\dots, \\underbrace{\\alpha\\beta_k + \\kappa}_{\\text{sticky diagonal}}, \\dots, \\alpha\\beta_L\\big)$$\n\nEach Gibbs sweep cycles four steps:\n\n**(a) Block-sample the path $z_{1:T}$ — this is the boundary-inferring step.** Forward filter, then sample backward:\n\n$$\\alpha_t(k) \\;\\propto\\; \\underbrace{\\mathcal{N}(x_t \\mid \\theta_k)}_{\\text{emission}} \\sum_{j=1}^{L} \\alpha_{t-1}(j)\\,\\underbrace{\\pi_j(k)}_{\\text{transition}}, \\qquad z_T \\sim \\alpha_T(\\cdot),\\;\\; z_t \\sim \\alpha_t(k)\\,\\pi_k(z_{t+1})$$\n\nBecause the whole path is sampled jointly, a boundary lands wherever the **emission evidence flips**, weighed against the sticky prior — coherently across the sequence, never frame-by-frame.\n\n**(b) Resample emissions** $\\theta_k$: gather
$\\{x_t : z_t = k\\}$, NIW posterior is closed-form.\n\n**(c) Resample transitions** $\\pi_k$: count $n_{jk} = \\#\\{t: z_{t-1}=j, z_t=k\\}$, then $\\pi_k \\sim \\text{Dir}(\\alpha\\beta + \\kappa\\delta_k + n_{k\\cdot})$.\n\n**(d) Resample $\\beta$** via the CRF auxiliary \"table counts\" — the same CRP table-counting bookkeeping from the very first problems (the sticky model adds \"override\" auxiliary variables to account for $\\kappa$; see Fox et al. for the exact form).\n\n---\n\n## Watching a Boundary Get Placed (concrete)\n\nTwo states — **A** ($\\mu_A = 0$) and **O** ($\\mu_O = 5$), 1-D emissions, $\\sigma=1$, sticky self-transition $0.9$. Six frames:\n\n$$x = [\\,0.2,\\; -0.1,\\; 0.5,\\;\\; 4.8,\\; 5.1,\\; 4.9\\,]$$\n\nPer-frame emission evidence (which state the Gaussian prefers, and by how much):\n\n| Frame | $x_t$ | favors | likelihood ratio |\n|---|---|---|---|\n| 1 | 0.2 | **A** | $\\sim 10^{5}$ |\n| 2 | −0.1 | **A** | $\\sim 10^{5}$ |\n| 3 | 0.5 | **A** | $\\sim 10^{4}$ |\n| 4 | 4.8 | **O** | $\\sim 10^{5}$ |\n| 5 | 5.1 | **O** | $\\sim 10^{5}$ |\n| 6 | 4.9 | **O** | $\\sim 10^{5}$ |\n\nFFBS pins the path to `A A A | O O O`, boundary after frame 3 — with overwhelming posterior, because the emission ratios ($10^4$–$10^5$) dwarf the $9{:}1$ sticky prior. Note two things: frame 2's wiggle ($-0.1$) does **not** trigger a spurious break (emissions still scream A, and stickiness reinforces it), and the single boundary *emerged* — at no point did anything compute a \"break rate.\"\n\n---\n\n## Why the Two Failures Dissolve — in the Math\n\n**Class imbalance.** The old objective was $\\prod_t \\text{Bernoulli}(b_t \\mid f(x_t))$ with $b_t\\in\\{0,1\\}$; at a 4–6% positive rate its optimum is the degenerate \"$b_t=0$ always.\" The new objective is\n\n$$p(x_{1:T}, z_{1:T}) = \\prod_t \\pi_{z_{t-1}}(z_t)\\,\\mathcal{N}(x_t \\mid \\theta_{z_t})$$\n\n— **there is no Bernoulli over breaks**, so there is no positive class to be rare. The break *rate* is now governed by the sticky prior $\\rho$ (a belief about dwell time), and it **cannot collapse to \"never switch,\"** because a single Gaussian forced to explain an acoustically varied song is punished brutally by the emission term $\\mathcal{N}(x_t\\mid\\theta_k)$. Emissions *force* changes where acoustics change; stickiness only suppresses *spurious* ones. Imbalance was an artifact of the framing, not a property of the problem.\n\n**Timing jitter.** The old loss conditioned on the subtitle timestamps $b_{1:T}$ as ground truth at 20-ms resolution — so a ±50 ms (±2–3 frame) label error *trained the model against wrong targets*, penalizing it for being right. In the HMM the boundaries are **latent**, inferred from $x_{1:T}$ (clean audio); the subtitle contributes only the syllable **sequence** $w_{1:M}$ (which units, in what order), a quantity that is *timestamp-independent and reliable*. In the toy: had the subtitle claimed the boundary after frame 2 (off by ~40 ms), the per-frame model would be taught \"frame 3 = O\" — wrong — while FFBS recovers the boundary after frame 3 from the acoustics, ignoring the bad timestamp. The noisy quantity is moved from *observed target* to *not used*; the alignment is sampled, as precise as the audio allows.\n\n---\n\n## Wiring In Your Subtitles (two modes) + the one refinement\n\n- **Mode A — robust forced alignment (best for transcription).** Constrain the path to a left-to-right chain visiting $w_1, \\dots, w_M$ in order, self-loops for duration. You're not discovering the inventory (you know the syllables); you're inferring only the *durations/boundaries* — exactly forced alignment, now immune to the ±50 ms.\n- **Mode B — unit discovery.** Run it unsupervised, let the HDP find phone-like units, then map them to Hiragana with the noisy labels as weak supervision — and find out whether 110 is even the right inventory.\n- **The refinement worth knowing:** a plain sticky HMM implies *geometric* dwell times (mode at duration 1 — not syllable-like). The **HDP-HSMM** (Johnson & Willsky) models segment durations *explicitly* (e.g., Poisson/negative-binomial), which matches \"syllables have a characteristic length\" directly. It's the natural upgrade once the sticky version is working.\n\n---\n\n## Key Takeaways\n\n1. **The reframe is the whole trick:** a break is $z_t \\neq z_{t-1}$, a derived event — not a classification target. That single shift is what kills the imbalance.\n2. **Four ingredients:** HDP stick-breaking gives an *unbounded* unit inventory ($\\beta$); the **sticky** $\\kappa$ enforces *persistence* (the cure for per-frame switching); a conjugate Gaussian emits the Wav2Vec2 (or contrastive) features; a Markov chain ties it together.\n3. **Inference is the early machinery, scaled up:** weak-limit + FFBS block-samples the path (boundaries placed jointly by emission evidence vs. sticky prior), conjugate updates for emissions/transitions, and CRP table-counts for $\\beta$.\n4. **Both failures dissolve formally:** imbalance vanishes because no Bernoulli-over-breaks exists and emissions prevent collapse; jitter vanishes because boundaries become latent and conditioning moves to the clean *sequence*, not the noisy *timestamps*.\n5. **It all interlocks:** contrastive features sharpen the emissions, the aligner supplies cover alignments back to the contrastive step, and the HSMM duration model is a one-line upgrade when you want syllable-shaped dwell times.\n\nThat's the plan turned into math you could implement: a sticky HDP-HMM (or HDP-HSMM) over contrastive-Wav2Vec2 features, FFBS for alignment, your subtitles as a sequence constraint. It attacks the exact two things your paper identified as fatal — and it does so with the CRP-to-Gibbs toolkit you started this whole thread on.\n\n/* CHAT LOG ENDS */ I want you to plan a comprehensive and concrete specifications/roadmap document that accomplishes the goals I laid out. Note that one or more of these goals could be accomplished at the same time (e.g., using XYZ approach to accomplish ABC task). No fluff, and focus on prioritizing the correct tasks first. Also, reassess how the discussed methods can be applied to improve the project; there's no need to follow what has been suggested so far, if there are better methods.
